import 'package:flutter/material.dart';

class ReportarIncidenciaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('IncidenciasPage'),
    );
  }
}